var <%= class_name %> = function(jsonObject) {
  var self = jsonObject || {};	
  self.location = '<%= "#{path}/" %>' + self.id;
  self.jsonLocation = self.location + '.json';
  self.edit = self.location + '/edit';

  return self;
}
